# Delta Base Styles

Colors definitions and hyperlink and heading styles.
No dependencies.

Add this class to your dependenceis as `delta_base_styles.cls` and import it as just `delta_base_styles` (no file extension), both properly referenced as a relative path from your template folder (use `../` as many times as necessary).